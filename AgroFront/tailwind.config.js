/** Minimal Tailwind config restored to default (no custom CSS variable mappings)
 * If you didn't have this file originally you can remove it.
 */
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
